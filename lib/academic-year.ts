/**
 * Année universitaire courante — SOURCE UNIQUE (serveur et navigateur).
 * À changer via la variable d'environnement NEXT_PUBLIC_CURRENT_ACADEMIC_YEAR
 * (ex. "2027-2028") à chaque rentrée, sans modifier le code.
 */
export const CURRENT_ACADEMIC_YEAR_ID = process.env.NEXT_PUBLIC_CURRENT_ACADEMIC_YEAR || '2026-2027';
